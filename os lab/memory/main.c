#include <stdio.h>
#include <stdlib.h>

struct process {
int id,s1,st,ft,f=0;
};

int mp;
void push(int id2,s3) {

}
int main()
{

    int s,i,ch,id1,s2;
    printf("Enter the no. of memory blocks:");
    scanf("%d",&s);
    int m[s];
    printf("Enter the size of memory blocks:");
    for(i=0;i<s;i++) {
        scanf("%d",&m[i]);
    }
    printf("1.New Process 2.Delete");
    scanf("%d",&ch);
    switch(ch) {
     case 1: printf("Enter process id: ");
             scanf("%d",&id1);
             printf("Enter process size: ");
             scanf("%d",&s2);


    }
    return 0;
}
